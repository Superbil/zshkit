path=(/usr/libexec/ $path)

# Allow Brew path
path=(/usr/local/sbin /usr/local/bin $path)

# Set homebrew's manpath
if [[ -x `which brew` ]]; then
  manpath=(/usr/share/man /usr/local/share/man $manpath)
fi

